---
const { userName, userImage } = Astro.props;
---

<div
  title={`Imagen del candidato ${userName}`}
  class="card-background"
  style=`background-image: url(${userImage});`
>
</div>
<h3 class="card-heading">{userName}</h3>

<style>
  .card-background {
    width: 100%;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    border-radius: 0.5rem;
    background-position: center;
    background-size: cover;
  }

  .card-heading {
    font-size: 1.5rem;
    font-weight: 600;
  }
</style>
